title: "Nearest Address Finder"
description: "Find the N nearest addresses from a list of 2,000 — Solution Overview for Management"
author: "GitHub Copilot"

url: "https://arcon-x.github.io"
baseurl: "/Marius"

theme: minima

plugins:
  - jekyll-relative-links
  - jekyll-seo-tag

relative_links:
  enabled: true
  collections: true

markdown: kramdown
highlighter: rouge

kramdown:
  input: GFM
  syntax_highlighter: rouge

defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "default"
